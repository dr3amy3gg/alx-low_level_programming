#include "main.h"

/**
 * print_alphabet - prints alphabet lowercase
 *
 * Return: always 0
 */

void print_alphabet(void)
{
	char alpha;
	for (alpha = 'a'; a <= 'z'; alpha++)
	{
		_putchar(alpha);
	}
	_puthchar('\n');
	return (0);
}
